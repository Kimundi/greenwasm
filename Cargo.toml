cargo-features = ["edition"]

[package]
edition = '2018'
name = "greenwasm"
version = "0.1.0"
authors = ["Marvin Löbel <loebel.marvin@gmail.com>"]
license = "MIT/Apache-2.0"

description = "An implementation of the Webassembly spec in Rust."
readme = "README.md"

[dependencies]
greenwasm-structure = { path = "greenwasm-structure" }
greenwasm-validation = { path = "greenwasm-validation" }
greenwasm-binary-format = { path = "greenwasm-binary-format" }
greenwasm-execution = { path = "greenwasm-execution" }
